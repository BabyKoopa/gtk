# Pantalla con boton en GTK

Para compilar ejecute el siguiente comando

``` 
gcc `pkg-config --cflags gtk+-3.0` hello-world.c -o helloworldEXE `pkg-config --libs gtk+-3.0`
```

Para ejecutar

``` 
./helloworldEXE 
```
